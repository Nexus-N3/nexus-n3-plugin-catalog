# Movesense

Bundle-compatible migration of the legacy `movesense` sensor plugin.

This Phase 7 migration intentionally excludes the USB camera plugin and keeps
Movesense focused on the BLE ECG/HR/TEMP sensor path.
