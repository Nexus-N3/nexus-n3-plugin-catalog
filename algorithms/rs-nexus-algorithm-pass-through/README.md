# Pass Through

Bundle-compatible migration of the built-in `pass_through` algorithm.

This reference plugin preserves the existing pass-through behavior while
defaulting to a 5-second buffer window to reduce compatibility risk during the
Phase 7 migration.
