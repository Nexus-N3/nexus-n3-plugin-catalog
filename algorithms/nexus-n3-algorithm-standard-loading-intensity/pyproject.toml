[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexus-n3-algorithm-standard-loading-intensity"
version = "0.1.0"
description = "Standard Loading Intensity algorithm plugin for Nexus N3 OS"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "nexus-n3-plugin-sdk>=0.1.0",
  "numpy>=1.24",
  "scipy>=1.10",
]

[project.entry-points."nexus_n3.algorithms"]
standard_loading_intensity = "standard_loading_intensity.core:StandardLoadingIntensityAlgorithm"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
standard_loading_intensity = ["*.yaml", "plugin.json"]
