[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexus-n3-algorithm-ecg-rhythm"
version = "0.1.1"
description = "ECG rhythm metrics algorithm plugin for Nexus N3 OS"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "nexus-n3-plugin-sdk>=0.1.0",
  "numpy>=1.24",
  "scipy>=1.10",
]

[project.entry-points."nexus_n3.algorithms"]
ecg_rhythm = "ecg_rhythm.core:ECGRhythmMetricsAlgorithm"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ecg_rhythm = ["*.yaml", "plugin.json"]
